-- ================================
-- smartgram 設定ファイル
-- ================================
-- このファイルを編集して、あなたの設定に合わせてください

return {
    -- ================================
    -- プラン選択
    -- ================================
    -- "basic"    : タイムラインいいねのみ
    -- "standard" : いいね＋フォロー/アンフォロー
    -- "premium"  : 全機能（アクティブユーザーいいね含む）
    plan = "standard",  -- あなたのプランを入力

    -- ================================
    -- 動作設定
    -- ================================
    settings = {
        -- 1日の上限設定
        daily_limits = {
            likes = 500,      -- 1日のいいね上限
            follows = 100,    -- 1日のフォロー上限
            unfollows = 100,  -- 1日のアンフォロー上限
        },

        -- 動作間隔（秒）
        intervals = {
            like_min = 10,      -- いいね最小間隔
            like_max = 30,      -- いいね最大間隔
            follow_min = 30,    -- フォロー最小間隔
            follow_max = 60,    -- フォロー最大間隔
            scroll_min = 5,     -- スクロール最小間隔
            scroll_max = 10,    -- スクロール最大間隔
        },

        -- 動作時間帯（24時間表記）
        active_hours = {
            start = 9,   -- 開始時間（9時）
            stop = 23,   -- 終了時間（23時）
        },

        -- スキップ設定
        skip = {
            verified = false,    -- 認証済みアカウントをスキップ
            business = false,    -- ビジネスアカウントをスキップ
            private = true,      -- 非公開アカウントをスキップ
        },
    },

    -- ================================
    -- ターゲット設定（Premium限定）
    -- ================================
    targets = {
        -- ハッシュタグ（アクティブユーザーいいね用）
        hashtags = {
            "#instagram",
            "#instagood",
            "#photooftheday",
            -- 追加したいハッシュタグを記入
        },

        -- 競合アカウント（そのフォロワーをターゲット）
        competitor_accounts = {
            -- "@username1",
            -- "@username2",
        },

        -- 最小フォロワー数（これ以下はスキップ）
        min_followers = 100,

        -- 最大フォロワー数（これ以上はスキップ）
        max_followers = 10000,
    },

    -- ================================
    -- 安全設定
    -- ================================
    safety = {
        -- エラー時の再試行回数
        max_retries = 3,

        -- 連続エラー時の停止
        stop_on_errors = 5,

        -- アプリがクラッシュした時の自動再起動
        auto_restart = true,

        -- ランダム性を追加（bot検出回避）
        randomize = true,
    },

    -- ================================
    -- ログ設定
    -- ================================
    logging = {
        -- ログを保存するか
        enabled = true,

        -- ログレベル（debug/info/warning/error）
        level = "info",

        -- ログファイルパス
        file_path = "smartgram_log.txt",
    },

    -- ================================
    -- 通知設定
    -- ================================
    notifications = {
        -- 完了時に通知
        on_complete = true,

        -- エラー時に通知
        on_error = true,

        -- 1日の上限到達時に通知
        on_limit_reached = true,
    },
}